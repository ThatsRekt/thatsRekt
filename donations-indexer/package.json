{
  "name": "donations-indexer",
  "version": "0.1.0",
  "description": "Squid processor watching native-coin donations to the thatsrekt.eth Safe. Writes to thatsrekt_donations DB. Processor-only (no squid-graphql-server).",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "rm -rf lib && tsc",
    "process": "node lib/main.js",
    "test": "bun test"
  },
  "dependencies": {
    "@subsquid/evm-processor": "1.30.1",
    "@subsquid/util-internal-processor-tools": "4.4.0",
    "dotenv": "16.4.7",
    "pg": "8.13.1"
  },
  "devDependencies": {
    "@types/bun": "1.1.14",
    "@types/node": "22.13.4",
    "@types/pg": "8.11.10",
    "typescript": "5.7.3"
  }
}
