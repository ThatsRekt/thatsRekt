# Compiler output
cache/
out/

# Deployment broadcasts
broadcast/

# Env file (keep .env.example)
.env

# Forge install lockfile (deps are vendored; lockfile is local-only)
foundry.lock

# Anvil bootstrap output — local deploy state, varies per machine
script/anvil/.deployed.*.json
