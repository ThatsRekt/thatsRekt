# =============================================================================
# thatsRekt indexer — multi-squid compose stack (Phases 2 + 5).
# =============================================================================
# One Postgres process hosting one logical database per chain (provisioned
# by init.sql). One processor and one GraphQL service per chain, all built
# from the same image. Squid GraphQL ports are compose-internal only — the
# Mesh gateway is the single public surface.
#
# Chains:
#   anvil-eth  (chainId 31337)  — local Anvil fork of Sepolia
#   anvil-base     (chainId 31338)  — local Anvil fork of Base
#   sepolia        (chainId 11155111) — real Ethereum Sepolia
#   base           (chainId 8453)   — real Base mainnet
#   optimism       (chainId 10)    — real Optimism mainnet
#   bsc            (chainId 56)    — BNB Chain mainnet
#   polygon        (chainId 137)   — Polygon mainnet
#
# Service map per chain:
#   migrate-{slug}     one-shot migrations against thatsrekt_{slug}
#   processor-{slug}   long-running indexer
#   graphql-{slug}     squid GraphQL on internal port (4351-4355, 4358 for bsc, 4363 for polygon)
#
# Plus shared:
#   db                 postgres :5432  (host loopback only, 127.0.0.1)
#   mesh               GraphQL stitching gateway :4350 (the public surface)
#
# The two anvil-* chains depend on services in docker-compose.anvil.yml
# (the anvil overlay) for their RPC endpoints. Bring them up via:
#   docker compose -f docker-compose.yml -f docker-compose.anvil.yml up -d
# =============================================================================

x-indexer: &indexer
  build:
    context: .
    dockerfile: Dockerfile

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASS:-postgres}
    ports:
      - '127.0.0.1:${DB_PORT:-5432}:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/01-create-databases.sql:ro
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${DB_USER:-postgres}']
      interval: 5s
      timeout: 5s
      retries: 10

  # ===========================================================================
  # anvil-eth (chainId 31337)
  # ===========================================================================
  migrate-anvil-eth:
    <<: *indexer
    command: ['npx', 'squid-typeorm-migration', 'apply']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_anvil_eth
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
    restart: 'no'
    depends_on:
      db:
        condition: service_healthy

  processor-anvil-eth:
    <<: *indexer
    command: ['node', 'lib/main.js']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_anvil_eth
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      CHAIN: anvil-eth
      RPC_ANVIL_ETH_HTTP: ${RPC_ANVIL_ETH_HTTP:-http://anvil-eth:8545}
      CONTRACT_ANVIL_ETH: ${CONTRACT_ANVIL_ETH:-}
      START_BLOCK_ANVIL_ETH: ${START_BLOCK_ANVIL_ETH:-}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-anvil-eth:
        condition: service_completed_successfully

  graphql-anvil-eth:
    <<: *indexer
    command: ['npx', 'squid-graphql-server']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_anvil_eth
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      GQL_PORT: 4351
      NODE_ENV: development
    expose:
      - '4351'
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-anvil-eth:
        condition: service_completed_successfully

  # ===========================================================================
  # anvil-base (chainId 31338)
  # ===========================================================================
  migrate-anvil-base:
    <<: *indexer
    command: ['npx', 'squid-typeorm-migration', 'apply']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_anvil_base
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
    restart: 'no'
    depends_on:
      db:
        condition: service_healthy

  processor-anvil-base:
    <<: *indexer
    command: ['node', 'lib/main.js']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_anvil_base
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      CHAIN: anvil-base
      RPC_ANVIL_BASE_HTTP: ${RPC_ANVIL_BASE_HTTP:-http://anvil-base:8545}
      CONTRACT_ANVIL_BASE: ${CONTRACT_ANVIL_BASE:-}
      START_BLOCK_ANVIL_BASE: ${START_BLOCK_ANVIL_BASE:-}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-anvil-base:
        condition: service_completed_successfully

  graphql-anvil-base:
    <<: *indexer
    command: ['npx', 'squid-graphql-server']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_anvil_base
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      GQL_PORT: 4354
      NODE_ENV: development
    expose:
      - '4354'
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-anvil-base:
        condition: service_completed_successfully

  # ===========================================================================
  # sepolia (chainId 11155111)
  # ===========================================================================
  migrate-sepolia:
    <<: *indexer
    command: ['npx', 'squid-typeorm-migration', 'apply']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_sepolia
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
    restart: 'no'
    depends_on:
      db:
        condition: service_healthy

  processor-sepolia:
    <<: *indexer
    command: ['node', 'lib/main.js']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_sepolia
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      CHAIN: sepolia
      RPC_SEPOLIA_HTTP: ${RPC_SEPOLIA_HTTP:-}
      CONTRACT_SEPOLIA: ${CONTRACT_SEPOLIA:-}
      START_BLOCK_SEPOLIA: ${START_BLOCK_SEPOLIA:-}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-sepolia:
        condition: service_completed_successfully

  graphql-sepolia:
    <<: *indexer
    command: ['npx', 'squid-graphql-server']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_sepolia
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      GQL_PORT: 4352
      NODE_ENV: development
    expose:
      - '4352'
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-sepolia:
        condition: service_completed_successfully

  # ===========================================================================
  # base (chainId 8453)
  # ===========================================================================
  migrate-base:
    <<: *indexer
    command: ['npx', 'squid-typeorm-migration', 'apply']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_base
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
    restart: 'no'
    depends_on:
      db:
        condition: service_healthy

  processor-base:
    <<: *indexer
    command: ['node', 'lib/main.js']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_base
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      CHAIN: base
      RPC_BASE_HTTP: ${RPC_BASE_HTTP:-}
      CONTRACT_BASE: ${CONTRACT_BASE:-}
      START_BLOCK_BASE: ${START_BLOCK_BASE:-}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-base:
        condition: service_completed_successfully

  graphql-base:
    <<: *indexer
    command: ['npx', 'squid-graphql-server']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_base
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      GQL_PORT: 4353
      NODE_ENV: development
    expose:
      - '4353'
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-base:
        condition: service_completed_successfully

  # ===========================================================================
  # optimism (chainId 10)
  # ===========================================================================
  migrate-optimism:
    <<: *indexer
    command: ['npx', 'squid-typeorm-migration', 'apply']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_optimism
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
    restart: 'no'
    depends_on:
      db:
        condition: service_healthy

  processor-optimism:
    <<: *indexer
    command: ['node', 'lib/main.js']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_optimism
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      CHAIN: optimism
      RPC_OPTIMISM_HTTP: ${RPC_OPTIMISM_HTTP:-}
      CONTRACT_OPTIMISM: ${CONTRACT_OPTIMISM:-}
      START_BLOCK_OPTIMISM: ${START_BLOCK_OPTIMISM:-}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-optimism:
        condition: service_completed_successfully

  graphql-optimism:
    <<: *indexer
    command: ['npx', 'squid-graphql-server']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_optimism
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      GQL_PORT: 4355
      NODE_ENV: development
    expose:
      - '4355'
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-optimism:
        condition: service_completed_successfully

  # ===========================================================================
  # ethereum (chainId 1)
  # ===========================================================================
  migrate-ethereum:
    <<: *indexer
    command: ['npx', 'squid-typeorm-migration', 'apply']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_ethereum
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
    restart: 'no'
    depends_on:
      db:
        condition: service_healthy

  processor-ethereum:
    <<: *indexer
    command: ['node', 'lib/main.js']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_ethereum
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      CHAIN: ethereum
      RPC_ETHEREUM_HTTP: ${RPC_ETHEREUM_HTTP:-}
      CONTRACT_ETHEREUM: ${CONTRACT_ETHEREUM:-}
      START_BLOCK_ETHEREUM: ${START_BLOCK_ETHEREUM:-}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-ethereum:
        condition: service_completed_successfully

  graphql-ethereum:
    <<: *indexer
    command: ['npx', 'squid-graphql-server']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_ethereum
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      GQL_PORT: 4356
      NODE_ENV: development
    expose:
      - '4356'
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-ethereum:
        condition: service_completed_successfully

  # ===========================================================================
  # arbitrum (chainId 42161)
  # ===========================================================================
  migrate-arbitrum:
    <<: *indexer
    command: ['npx', 'squid-typeorm-migration', 'apply']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_arbitrum
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
    restart: 'no'
    depends_on:
      db:
        condition: service_healthy

  processor-arbitrum:
    <<: *indexer
    command: ['node', 'lib/main.js']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_arbitrum
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      CHAIN: arbitrum
      RPC_ARBITRUM_HTTP: ${RPC_ARBITRUM_HTTP:-}
      CONTRACT_ARBITRUM: ${CONTRACT_ARBITRUM:-}
      START_BLOCK_ARBITRUM: ${START_BLOCK_ARBITRUM:-}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-arbitrum:
        condition: service_completed_successfully

  graphql-arbitrum:
    <<: *indexer
    command: ['npx', 'squid-graphql-server']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_arbitrum
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      GQL_PORT: 4357
      NODE_ENV: development
    expose:
      - '4357'
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-arbitrum:
        condition: service_completed_successfully

  # ===========================================================================
  # bsc (chainId 56)
  # ===========================================================================
  migrate-bsc:
    <<: *indexer
    command: ['npx', 'squid-typeorm-migration', 'apply']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_bsc
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
    restart: 'no'
    depends_on:
      db:
        condition: service_healthy

  processor-bsc:
    <<: *indexer
    command: ['node', 'lib/main.js']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_bsc
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      CHAIN: bsc
      RPC_BSC_HTTP: ${RPC_BSC_HTTP:-}
      CONTRACT_BSC: ${CONTRACT_BSC:-}
      START_BLOCK_BSC: ${START_BLOCK_BSC:-}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-bsc:
        condition: service_completed_successfully

  graphql-bsc:
    <<: *indexer
    command: ['npx', 'squid-graphql-server']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_bsc
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      GQL_PORT: 4358
      NODE_ENV: development
    expose:
      - '4358'
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-bsc:
        condition: service_completed_successfully

  # ===========================================================================
  # polygon (chainId 137)
  # ===========================================================================
  migrate-polygon:
    <<: *indexer
    command: ['npx', 'squid-typeorm-migration', 'apply']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_polygon
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
    restart: 'no'
    depends_on:
      db:
        condition: service_healthy

  processor-polygon:
    <<: *indexer
    command: ['node', 'lib/main.js']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_polygon
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      CHAIN: polygon
      RPC_POLYGON_HTTP: ${RPC_POLYGON_HTTP:-}
      CONTRACT_POLYGON: ${CONTRACT_POLYGON:-}
      START_BLOCK_POLYGON: ${START_BLOCK_POLYGON:-}
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-polygon:
        condition: service_completed_successfully

  graphql-polygon:
    <<: *indexer
    command: ['npx', 'squid-graphql-server']
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: thatsrekt_polygon
      DB_USER: ${DB_USER:-postgres}
      DB_PASS: ${DB_PASS:-postgres}
      GQL_PORT: 4363
      NODE_ENV: development
    expose:
      - '4363'
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      migrate-polygon:
        condition: service_completed_successfully

  # ===========================================================================
  # Mesh gateway — single public GraphQL surface stitching all 9 squid APIs.
  # ===========================================================================
  mesh:
    build:
      context: ../mesh
      dockerfile: Dockerfile
    environment:
      PORT: ${MESH_PORT:-4350}
      MESH_CHAINS: ${MESH_CHAINS:-anvil-eth,anvil-base,sepolia,ethereum,base,optimism,arbitrum,bsc,polygon}
      GRAPHQL_ANVIL_ETH_URL: http://graphql-anvil-eth:4351/graphql
      GRAPHQL_ANVIL_BASE_URL: http://graphql-anvil-base:4354/graphql
      GRAPHQL_SEPOLIA_URL: http://graphql-sepolia:4352/graphql
      GRAPHQL_ETHEREUM_URL: http://graphql-ethereum:4356/graphql
      GRAPHQL_BASE_URL: http://graphql-base:4353/graphql
      GRAPHQL_OPTIMISM_URL: http://graphql-optimism:4355/graphql
      GRAPHQL_ARBITRUM_URL: http://graphql-arbitrum:4357/graphql
      GRAPHQL_BSC_URL: http://graphql-bsc:4358/graphql
      GRAPHQL_POLYGON_URL: http://graphql-polygon:4363/graphql
    ports:
      - '127.0.0.1:${MESH_PORT:-4350}:${MESH_PORT:-4350}'
    restart: unless-stopped
    depends_on:
      graphql-anvil-eth:
        condition: service_started
      graphql-anvil-base:
        condition: service_started
      graphql-sepolia:
        condition: service_started
      graphql-ethereum:
        condition: service_started
      graphql-base:
        condition: service_started
      graphql-optimism:
        condition: service_started
      graphql-arbitrum:
        condition: service_started
      graphql-bsc:
        condition: service_started
      graphql-polygon:
        condition: service_started

volumes:
  pgdata:
